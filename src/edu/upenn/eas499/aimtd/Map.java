package edu.upenn.eas499.aimtd;

/**
 * The TD map.
 * @author fedenusy
 *
 */
class Map {

	// Instance variables
	Tile[][] tiles;
	
	/**
	 * Instantiates the TD map.
	 * @param layout The layout of the map represented as an array of arrays. 0s represent
	 * Field tiles (upon which towers can be built), 1s represent Road tiles (upon which
	 * monsters can travel), and 2s represent Rock tiles (dead space).
	 */
	public Map(int[][] layout) {
		
	}
}
